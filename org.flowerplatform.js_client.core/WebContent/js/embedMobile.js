var logger = {
	debug: function(msg) {
		// trace in flex console?
	}
}

///**
// * Invokes a callback function on the Flex mobile app.
// * 
// * @param callback
// * @param args
// */
//function callFlexCallback(callback /* : String */, ...args) {
//	logger.debug("call flex: " + callback);
//	logger.debug("args: " + args);
//	StageWebViewBridge.call(callback, null, ...args);
//}